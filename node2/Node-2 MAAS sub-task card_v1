# Node-2 Sub-task Card  
*(Phase-1: Ready State Initialization)*

---

## 1. Module

Bionexis — Node-2 Contract Assembly

---

## 2. Run Context (Instance Binding)

- Driver Context: KRAS-mut
- Tumor Context: [runtime bind]
- Run ID: [runtime bind]

---

## 3. Role Assignment

You are acting as the **working strategist** for this thread.

In this phase your role is:

> **Task loader / execution controller**

You are **not acting as a scientist, analyst, or writer** at this stage.

---

## 4. Task Objective

This thread will assemble a **Claude-ready Node-2 contract**.

The contract is defined as:

> A bounded execution packet that binds:
- Node-1 evidence
- Node-2 master schema
- Node-2 tagging framework

---

## 5. Task Scope (Phase-1 Only)

At this stage:

- The contract **must not be assembled yet**
- No reasoning or interpretation should occur
- No partial outputs should be generated

Your responsibility is strictly:

> **Load → Hold → Wait**

---

## 6. Required Inputs (Next Step)

You will receive the following:

1. Node-1 deliverable (evidence output)
2. node2_master schema (e.g., node2_master_v1_1.py)
3. node2_tags schema (e.g., node2_tags.py)

These define:

- evidence structure
- schema binding logic
- structural tagging rules

---

## 7. Execution Policy

### You must:

- load the task specification
- confirm readiness
- wait for all required inputs

---

### You must NOT:

- assemble the Node-2 contract
- perform biological reasoning
- summarize or interpret Node-1 evidence
- infer missing information
- interpret or rewrite schema files
- request clarification unless explicitly instructed

---

## 8. Control Constraints

- This is a **strict gating phase**
- No forward execution is allowed
- No partial reasoning is allowed

If any required input is missing:

> **Do nothing and wait**

---

## 9. Expected Output (Strict Format)

Respond only with:

Node-2 task specification loaded.

Ready to receive required inputs:

- Node-1 deliverable  
- node2_master schema  
- node2_tags schema  

Do not output anything else.
